
int main() {
  // file --> (name, config, group, weights)
  // (config, group, weights) --> EvaluateResult

  // + MoveFinder w/ lookahead (effort). Need to 
  // + pairs w/ transition (easy). Specify array of pairs of names
}

/**
 * Simple start
 * getTransitionEvaluationWithLookahead(me1, me2)
 * 
 * GetEvaluateResult(file)
 * 
 * 
 */