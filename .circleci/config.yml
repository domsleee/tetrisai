version: 2
jobs:
  build:
    docker:
      - image: domslee/bazel:tbb
    steps:
      - checkout
      - run: >
          apt-get install software-properties-common -y;
          add-apt-repository ppa:jonathonf/gcc-9.0 -y;
          apt install gcc-9 g++-9 -y;
          update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-9 60 --slave /usr/bin/g++ g++ /usr/bin/g++-9;
          bazel build //src:ew_get_moves;
          mkdir persist;
          cp -r /usr/local/include/tbb_build persist/tbb_build;
          cp -r /usr/local/include/tbb persist/tbb;
          cp -r bazel-bin/src persist/src;
      - persist_to_workspace:
          root: persist
          paths:
            - src
            - tbb_build
            - tbb
  getMoveServiceTest:
    docker:
      - image: domslee/bazel:tbb
    steps:
      - checkout
      - attach_workspace:
          at: /tmp/persist
      - run: >
          mkdir -p /usr/local/include;
          sudo cp -r /tmp/persist/tbb_build /usr/local/include/tbb_build;
          sudo cp -r /tmp/persist/tbb /usr/local/include/tbb;
          sudo cp /tmp/persist/src/ew_get_moves .docker/get-move-service/src/external_bin/
      - run: >
          apt update;
          apt install python -y;
          cd .docker/get-move-service;
          sudo pip install pipenv;
          pipenv install;
          find /usr/local/include;
          pipenv run sh -c "cd src/test; python -m unittest";
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - getMoveServiceTest:
          requires:
            - build

